🛠️ Repair Service – модуль учёта заявок на ремонт климатического оборудования
Небольшой учебный проект на Django для учёта заявок на ремонт климатического оборудования.
Приложение позволяет создавать, редактировать и просматривать заявки, назначать исполнителей, а также получать статистику по выполненным работам 🚀

📦 Установка и запуск проекта
1. Клонирование репозитория
Открой Visual Studio Code.

В меню выбери:
… → Terminal → New Terminal (желательно терминал cmd, а не PowerShell).

Выполни команду:


    git clone https://github.com/lestanya/REPAIR-SERVICE-git.git
    cd REPAIR-SERVICE-git

    
2. Виртуальное окружение и зависимости
В проекте уже есть папка venv, но при необходимости окружение можно пересоздать:

        python -m venv venv

   
Активировать окружение:

Windows:

 
    venv\Scripts\activate

    
Linux / macOS:

  
    source venv/bin/activate

    
Установить зависимости:


    pip install -r requirements.txt

    
В корне проекта лежит удобный бат‑скрипт start.bat, который активирует виртуальное окружение и устанавливает все зависимости автоматически.
Из корня проекта достаточно выполнить:


    start.bat

    
3. Запуск Django‑проекта
Вариантов два 👇

Вариант 1. Через django.bat (консольное меню)
В корне проекта запусти:

    django.bat

    
Появится меню с пунктами:

Запуск сервера – стартует Django‑сервер.

После запуска перейди в браузере по адресу:
http://127.0.0.1:8000/

Остановка сервера: Ctrl + C в терминале.

Миграции – использовать при изменении моделей Django.

Применение миграций – фиксирует изменения в базе.

Просмотр базы данных – открывает инструмент просмотра БД.

Создание суперпользователя – запускает стандартную команду createsuperuser.

Дополнительные команды Django – вспомогательное меню для часто используемых команд.
Q – выход из меню.

Этот вариант удобен для быстрой ручной работы с проектом через консоль.

Вариант 2. Через Django Debugger в VS Code
В левой панели VS Code нажми на значок «Run and Debug» (треугольник с жуком).

В выпадающем списке конфигураций выбери Django (venv auto).

Нажми F5 для запуска отладки.

Сервер поднимется в режиме отладки, и приложение будет доступно по адресу:

(http://127.0.0.1:8000/)


🔐 Панель администратора
Для входа в административную панель Django:

Перейди в браузере по адресу:


http://127.0.0.1:8000/admin/

Введи логин и пароль ранее созданного суперпользователя (через пункт меню 5 в django.bat или команду python manage.py createsuperuser).

Через админ‑панель можно:

добавлять и редактировать заявки;

управлять пользователями и их ролями;

просматривать связанные сущности (комментарии, исполнители и т.д.).

💡 Что делает проект
регистрирует заявки на ремонт климатического оборудования с указанием типа, модели и описания проблемы;

хранит данные о заказчиках и исполнителях, поддерживает разные роли пользователей;

позволяет менять статусы заявок и добавлять комментарии по ходу выполнения работ;

рассчитывает и показывает статистику: количество выполненных заявок, среднее время ремонта, распределение по типам неисправностей.
